# Copy to .env before running docker compose.
# LLM_API_KEY is read by the backend (see backend/app/llm.py) — the LLM
# integration is not wired up yet; that's part of the exercise.
LLM_API_KEY=your-key-here
